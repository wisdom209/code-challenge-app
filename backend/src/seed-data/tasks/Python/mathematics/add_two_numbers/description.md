Add Two Numbers in Python
Write a function that adds two integers.
Function Signature
python
12
def add_two_numbers(a: int, b: int) -> int:
    pass
Requirements
Take two integers, return their sum
Handle positive, negative, and zero values
Use only built-in Python
Examples

add_two_numbers(5, 3)      # 8
add_two_numbers(-5, 5)     # 0
add_two_numbers(0, 0)      # 0
add_two_numbers(-10, -20)  # -30

